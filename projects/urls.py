from django.conf.urls import url
from django.urls import path
from django.conf.urls import include
from . views import *

urlpatterns = [
    # path('/', , ),
    # path('add/', ProjectView.as_view(), name='project_add'),
    # path('<int:id>/edit/', ProjectView.as_view(), name='project_edit'),
    # path('<int:id>/delete/', ProjectView.as_view(), name='project_delete'),
    # path('list/', index, name='projects_list'),
    # path('<int:id>/details/', details, name="project_details"),
    # path('<int:id>/', vote_project, name="project_vote")
]
